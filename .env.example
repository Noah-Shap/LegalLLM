# CourtListener API token (required for pipeline)
CL_TOKEN=your_token_here

# Tesseract OCR data directory (optional, for OCR features)
TESSDATA_PREFIX=/path/to/tessdata

# Anthropic API key (required for the LLM extractor, legallm-extract --method llm-v1)
ANTHROPIC_API_KEY=your_key_here
